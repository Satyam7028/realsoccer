# Root .env.example
# This file is for general project-wide environment variables.
# Specific variables for client and server are in their respective .env.example files.

# Node environment (e.g., development, production, test)
NODE_ENV=development

# --- Docker Compose related variables ---
# Port for the client application
CLIENT_PORT=3000
# Port for the server API
SERVER_PORT=5001
# Port for the MongoDB database
MONGO_PORT=27017

# --- Database connection (if not using Docker for dev) ---
# MONGODB_URI=mongodb://localhost:27017/realsoccer_db