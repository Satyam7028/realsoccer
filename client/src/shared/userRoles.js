// shared/constants/userRoles.js
/**
 * Defines the user roles available in the application.
 * These roles should be consistent between frontend and backend.
 */
const USER_ROLES = {
  USER: 'user',
  ADMIN: 'admin',
};

module.exports = USER_ROLES;