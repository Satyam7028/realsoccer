# Client .env.example
# This file contains environment variables specific to the frontend client.
# Variables prefixed with REACT_APP_ are exposed to the client-side code.

# Node environment (development, production)
NODE_ENV=development

# Base URL for the backend API
# If running the server locally:
# REACT_APP_API_URL=http://localhost:5000/api
# If running the server via Docker Compose:
# REACT_APP_API_URL=http://localhost:5000/api # This assumes Docker Compose maps server port 5000 to host 5000
REACT_APP_API_URL=http://localhost:5001/api

# Add any other client-specific environment variables here,
# prefixed with REACT_APP_ if they need to be accessible in the browser.