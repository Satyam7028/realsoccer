# Server .env.example
# This file contains environment variables specific to the backend server.

# Node environment (development, production, test)
NODE_ENV=development

# Port for the server to listen on
PORT=5001

# MongoDB connection URI
# If running MongoDB via Docker Compose, this will be:
# MONGO_URI=mongodb://mongo_username:mongo_password@mongodb:27017/realsoccer_db?authSource=admin
# If running MongoDB locally, it might be:
# MONGO_URI=mongodb://localhost:27017/realsoccer_db
MONGO_URI=mongodb://localhost:27017/realsoccer_db

# JSON Web Token (JWT) Secret Key
# This should be a long, random string.
JWT_SECRET=ea9f72a0219d04cfc1953e50fbf0b77b46bcf29968dd42337b15c9df5802ade5

# JWT Expiration Time (e.g., 1h, 7d, 30m)
JWT_EXPIRES_IN=1h

# Email service configuration (if applicable, for password resets, etc.)
# EMAIL_HOST=smtp.example.com
# EMAIL_PORT=587
# EMAIL_USER=your_email@example.com
# EMAIL_PASS=your_email_password
# EMAIL_FROM=Real Soccer <noreply@realsoccer.com>

# Add any other server-specific environment variables here